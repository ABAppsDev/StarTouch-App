package data.remote.mapper

import data.remote.model.FireItemsDto
import domain.entity.FireItems

fun FireItemsDto.toEntity() = FireItems(
    id = id ?: 0,
    price = price ?: 0f,
    qty = qty ?: 0,
    totalPrice = totalPrice ?: 0f,
    modifierGroupID = modifierGroupID ?: 0,
    isModifier = isModifier ?: false,
    noServiceCharge = noServiceCharge ?: false,
    ws = ws ?: 0,
    pickFollowItemQty = pickFollowItemQty ?: false,
    prePaidCard = prePaidCard ?: false,
    taxable = taxable ?: false,
    name = name,
    pOnReport = pOnReport,
    numFired = numFired,
    netPrice = netPrice,
    numPrint = numPrint,
    compReasonComment = compReasonComment,
    endSerialCount = endSerialCount,
    compBy = compBy,
    checkId = checkId,
    checkIdCombine = checkIdCombine,
    complement = complement,
    compReasonId = compReasonId,
    voidReasonComment = voidReasonComment,
    pOnCheck = pOnCheck,
    voidEffectInvn = voidEffectInvn,
    officer = officer,
    discountValue = discountValue,
    discountId = discountId,
    discReasonComment = discReasonComment,
    roundCheckFired = roundCheckFired,
    unitPrice = unitPrice,
    origPrice = origPrice,
    voidedTime = voidedTime,
    voidedBy = voidedBy,
    voidedReason = voidedReason,
    voided = voided,
    taxValue = taxValue,
    status = status,
    holdTime = holdTime,
    hold = hold,
    refAssItem = refAssItem,
    refModItem = refModItem,
    serverId = serverId,
    serial = serial,
    userId = userId,
    isAssembly = isAssembly,
    discBy = discBy,
    discReasonId = discReasonId,
    modifierGroupId = modifierGroupID,
    fired = fired,
    adjValue = adjValue,
    promoId = promoId,
    itemId = itemId,
    firedTime = firedTime,
    modifierPick = modifierPick,
    preparationTime = preparationTime,
)


fun FireItems.toDto() =
    FireItemsDto(
        id,
        name,
        price,
        totalPrice,
        qty,
        noServiceCharge,
        isModifier,
        taxable,
        pickFollowItemQty,
        modifierGroupID,
        prePaidCard,
        ws,
        pOnReport,
        checkId,
        itemId,
        unitPrice,
        discountValue,
        taxValue,
        adjValue,
        netPrice,
        serial,
        fired,
        firedTime,
        voided,
        voidedTime,
        voidedReason,
        pOnCheck,
        complement,
        status,
        refModItem,
        isAssembly,
        refAssItem,
        numFired,
        numPrint,
        serverId,
        checkIdCombine,
        roundCheckFired,
        voidEffectInvn,
        promoId,
        origPrice,
        officer,
        compReasonId,
        endSerialCount,
        discountId,
        discReasonId,
        hold,
        holdTime,
        voidedBy,
        compBy,
        discBy,
        preparationTime,
        discReasonComment,
        compReasonComment,
        voidReasonComment,
        modifierGroupId,
        modifierPick,
        userId
    )